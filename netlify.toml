[build]
  command = "NODE_ENV=production next build"
  publish = ".next"
  
[build.environment]
  NODE_ENV = "production"
  NEXT_PRIVATE_STANDALONE = "true"

[[plugins]]
  package = "@netlify/plugin-nextjs"
